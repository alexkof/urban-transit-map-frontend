import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Removed output: "export" for Vercel deployment to enable full Next.js features
  reactStrictMode: false,
};

export default nextConfig;
